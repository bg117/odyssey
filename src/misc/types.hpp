#pragma once

#include <cstdint>

using memory_address   = uint64_t;
using physical_address = memory_address;
using virtual_address  = memory_address;
using counter          = uint64_t;
using offset           = uint64_t;
